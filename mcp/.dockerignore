# ===== 最高优先级：绝对机密（坚决不打包） =====
.env          # 包含真实密钥、密码、Token，绝对禁止进镜像！
# config.py     # 如果里面写死了数据库连接串或API Key，必须排除（通过挂载注入）

# ===== Python 运行时缓存（垃圾，增加体积） =====
__pycache__/
*.pyc
*.pyo
*.pyd

# ===== 构建与部署辅助文件（跟运行无关） =====
# Dockerfile
.dockerignore
.env.example  # 虽然没密钥，但容器里不需要这个模板，保持镜像精简